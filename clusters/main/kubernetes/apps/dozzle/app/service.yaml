# service.yaml
apiVersion: v1
kind: Service
metadata:
  name: dozzle-service
  namespace: default
spec:
  type: LoadBalancer
  selector:
    app: dozzle
  ports:
    - port: 8080
      targetPort: 8080
      LoadBalancerIP: 192.168.0.247
      protocol: TCP