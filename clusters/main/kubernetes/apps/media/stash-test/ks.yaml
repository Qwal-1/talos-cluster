apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: stash-test
  namespace: flux-system
spec:
  interval: 10m
  path: clusters/main/kubernetes/apps/media/stash-test/app
  prune: true
  sourceRef:
    kind: GitRepository
    name: cluster

