machine:
  network:
    extraHostEntries:
      - ip: ${WEBSITE1_IP}
        hostnames:
          - ${WEBSITE1_NAME}
          - ${WEBSITE2_NAME}
      - ip: ${WEBSITE2_IP}
        hostnames:
          - ${WEBSITE3_NAME}
          - ${WEBSITE4_NAME}
      - ip: ${WEBSITE3_IP}
        hostnames:
          - ${WEBSITE3_NAME}
          - ${WEBSITE4_NAME}
