machine:
  network:
    extraHostEntries:
      - ip: ${WEBSITE1_IP}
        aliases:
          - ${WEBSITE1_NAME}
          - ${WEBSITE2_NAME}
      - ip: ${WEBSITE2_IP}
        aliases:
          - ${WEBSITE3_NAME}
          - ${WEBSITE4_NAME}
      - ip: ${WEBSITE3_IP}
        aliases:
          - ${WEBSITE3_NAME}
          - ${WEBSITE4_NAME}
